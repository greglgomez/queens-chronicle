---
import PlotlineCard from './PlotlineCard.astro';

interface Card {
  id: string;
  sequence: number;
  title: string;
  narrative: string | null;
  unlockedInGame: number;
}

interface Props {
  code: string;
  name: string;
  cards: Card[];
}

const { code, name, cards } = Astro.props;
const sorted = [...cards].sort((a, b) => a.sequence - b.sequence);
---

<section id={`thread-${code}`}>
  <h2>{name}</h2>
  {sorted.map((card) => (
    <PlotlineCard
      sequence={card.sequence}
      title={card.title}
      narrative={card.narrative}
      unlockedInGame={card.unlockedInGame}
    />
  ))}
</section>
